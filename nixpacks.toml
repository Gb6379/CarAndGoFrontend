[phases.setup]
nixPkgs = ["nodejs_18", "npm-8_x"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build:simple", "ls -la dist"]

[start]
cmd = "PORT=${PORT:-3001} npx serve -s dist -p $PORT"
